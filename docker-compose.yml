version: '3.7'
services:
    main:
        image: docker.pupuk-kujang.co.id/dwidev/empaapi:latest
        ports:
            - '8094:4000'
        restart: always
        environment:
            - TZ=Asia/Jakarta
        env_file:
            - .env
